# TrackB 기술 의사결정 로그

> 각 의사결정은 **선택지 → 선택 → 전략적 근거** 구조로 기록한다.
> 전략적 근거는 `AINOS_TrackB_전략정리_v3.md`의 프레임워크에 기반한다.
>
> **핵심 프레임워크 요약:**
> - **문제**: 진단-판매 미분리. 두 층이 있다 — **주체의 미분리**(진단자=판매자, 이해충돌)와 **순서의 미분리**(진단 단계 자체가 프로세스에 부재, 납득 없는 가입)
> - **해법**: 주체의 미분리 → AI가 해소(필요조건) / 순서의 미분리 → 리스크 프로파일링 기반 순서 강제가 해소(충분조건)
> - **기능 셋**: 리스크 프로파일링(§4-1) → 보장 갭 분석(§4-2) → 상품 매칭(§4-3) → 설계 근거 리포트(§4-4)
> - **보험사 가치**: 유지율 향상, 민원 감소, 금소법 설명의무 자동 이행 (§6)

---

## 1. 진단-판매 분리를 대화 순서로 구현

**선택지:**
- (A) AI에게 "좋은 상담을 해라"라고만 지시 → 진단-판매 순서는 AI 재량
- (B) STAGE 1~3 완료 전까지 상품명 언급을 시스템 프롬프트에서 금지 → 순서를 구조적으로 강제

**선택:** (B) 시스템 프롬프트 레벨에서 상품 등장 시점을 강제 차단.

**전략적 근거:** §2-2에서 순서의 미분리를 독립 문제로 정의했다. "진단이라는 독립된 단계 자체가 프로세스에 존재하지 않는다"가 문제이므로, 해법은 그 단계를 프로세스에 강제로 삽입하는 것이다(§3-2). (A)는 AI가 이해충돌이 없다는 주체의 미분리만 해소하고(§3-1), 순서의 미분리를 남겨둔다. AI에 이해충돌이 없어도 진단 단계가 강제되지 않으면, 고객은 여전히 리스크를 모른 채 상품을 선택한다. 필요조건만으로는 납득 있는 가입이 완성되지 않는다.

---

## 2. 리스크 점수를 통계청 데이터 기반 수식으로 설계

**선택지:**
- (A) 브라켓 기반 점수 산출 (예: "30대 → 4~5점") — 구현 간단, 근거 불투명
- (B) 통계청 실제 데이터를 수식에 직접 대입 — 구현 복잡, 근거 투명

**선택:** (B) 통계청 데이터(사망원인통계, 생명표, 산업재해통계, 경제활동인구조사, 건강검진통계) 기반 수식.

**최종 수식 구조:**
- **사망**: `(dependency_factors / 0.8 + 사망확률 기여분) × 소득 배수(0.7~1.6)`
- **질병**: `log10(3대 질병 사망률 합산 + 1) × 2.5 × 가족력 배수 + 현재 질환 가산점`
- **상해**: `occupational_risks[직업].위험도` 직접 조회
- **소득중단**: `11 - employment_stability[고용형태].stability_score`
- **노후**: `나이대별 base점수(4~9) - 연금준비 감소분(0~3)`

**전략적 근거:** §4-1에서 "왜 7점인데?"에 "국가 통계 기준입니다"로 답할 수 있어야 진단의 객관성이 확보된다고 했다. (A)처럼 브라켓 기반이면 "30대니까 4~5점"이라는 설명밖에 못 하고, 이는 설계사의 주관적 판단과 다를 바 없다. 주체의 미분리를 AI로 해소한 의미(§3-1)가, 진단 근거가 불투명하면 퇴색된다 — AI든 설계사든 판단 근거를 설명 못 하기는 마찬가지가 되기 때문이다. 또한 진단 데이터(통계청)와 판매 데이터(KB라이프 공시)가 소스 자체부터 분리되어, 데이터 레벨에서도 진단-판매 분리가 실현된다(§4-1).

---

## 3. 리스크 점수에 성별·소득·현재 질환 반영

**선택지:**
- (A) 성별 미반영, 소득 배수 0.8~1.2, 현재 질환 미반영 — 변수 적어 단순
- (B) 성별 분리, 소득 배수 0.7~1.6, 현재 질환 4단계(없음/경증/중증/암이력) — 변수 많지만 정밀

**선택:** (B) 세 변수 모두 반영.

**전략적 근거:** §4-1에서 진단 방법을 "통계청 데이터 기반 객관적 진단"이라 정의했고, 해당 데이터는 연령/성별 분리돼 있다. 데이터가 분리돼 있는데 진단에 반영하지 않으면 "통계 기반"이라는 주장의 신뢰성이 무너진다. 소득은 사망/유고 리스크(§4-1 리스크 카테고리: "부양가족, 소득 의존도")의 핵심 변수이고, 현재 질환은 보험 가입 심사의 핵심 요인이다. 이 변수 없이는 AI의 진단이 현실의 보험 심사와 괴리되어, 진단이 실질적이지 않게 된다.

---

## 4. 보장 갭 분석에 과잉보장 시각화 추가

**선택지:**
- (A) 보장 부족(갭)만 표시 — "여기 더 가입하세요" 메시지
- (B) 보장 부족 + 과잉보장 동시 표시 — "불필요한 보험 해지하고 필요한 곳에 재배치하세요" 메시지

**선택:** (B) 과잉보장을 주황, 보장 부족을 빨강으로 동시 시각화.

**전략적 근거:** §4-2에서 "과잉보장 영역도 동시에 가시화"를 명시했다. (A)는 갭만 보여주므로 "추가 가입을 유도하는 도구"가 되고, 이는 §2-1의 주체 미분리 문제를 AI에서도 재현하는 셈이다 — 진단이 판매 정당화 도구로 전락한다. 과잉보장을 함께 보여줘야 비로소 중립적 진단이 되며, "이 지점에서 납득이 발생한다"(§4-2)는 것은 부족과 과잉을 모두 인식할 때 성립한다.

---

## 5. covered 점수를 기준표로 명시화 → 이후 툴화 (→ #14)

**선택지:**
- (A) AI 재량에 맡김 — "암보험 있으면 질병 커버 적당히 반영해"
- (B) 기준표를 시스템 프롬프트에 명시 — "암보험 → 질병 4점, 종신보험 → 사망 6점"

**선택:** (B) 기준표 명시. 이후 #14에서 결정론적 툴로 완전 이관.

**전략적 근거:** §6-3에서 금소법 설명의무 자동 이행을 보험사 가치로 제시했다. "리스크 프로파일링 → 갭 분석 → 상품 매칭의 구조화된 프로세스 자체가 설명의무의 실질적 이행 증거"가 되려면, 갭 분석의 결과가 재현 가능해야 한다. (A)처럼 AI 재량이면 동일 입력에 매번 다른 결과가 나올 수 있고, 이는 "구조화된 프로세스"라는 주장을 약화시킨다.

---

## 6. 스트리밍 응답 구현

**선택지:**
- (A) 응답 완료 후 일괄 전달 — 구현 간단
- (B) 청크 단위 실시간 스트리밍 — 구현 복잡, 체감 반응 빠름

**선택:** (B) 스트리밍.

**전략적 근거:** §5의 유저 여정이 9단계로, 매 단계마다 에이전트가 통계 근거를 인용하며 긴 설명을 생성한다. "납득 있는 가입"의 전제는 고객이 진단 결과와 설계 근거를 끝까지 읽는 것인데, 응답이 수 초간 빈 화면이면 이탈이 발생한다. 특히 STAGE 2(리스크 프로파일링, §4-1)에서 "왜 이 점수가 나왔는지" 통계 수치를 인용하는 텍스트가 긴데, 이 단계의 이탈은 순서 강제(§3-2) 자체의 실패를 의미한다.

---

## 7. 토큰 관리 (메시지 히스토리 + 데이터 조건부 로딩)

**선택지:**
- (A) 전체 히스토리 + 모든 데이터를 매번 전송
- (B) 최근 10개 메시지 + Stage별 필요 데이터만 조건부 전송

**선택:** (B) Stage 1: 데이터 없음 / Stage 2~3: 통계만 / Stage 4+: 통계+상품.

**전략적 근거:** §5의 9단계 유저 여정은 긴 히스토리를 만든다. 순서 강제(§3-2)를 위해 단계를 줄일 수는 없으므로, 토큰 효율로 해결해야 한다. Stage별 데이터 분리는 진단-판매 분리의 논리 구조와도 일치한다: Stage 1~3(진단 단계)에서는 통계 데이터(진단 데이터)만, Stage 4+(판매 단계)에서는 상품 데이터(판매 데이터)가 추가된다. §4-1에서 "진단 데이터(통계청)와 판매 데이터(KB라이프 공시)가 소스 자체부터 분리됨"이라 했는데, 이것이 토큰 관리에서도 자연스럽게 적용된다.

---

## 8. 세션 localStorage 유지

**선택지:**
- (A) 매번 새 세션 시작
- (B) localStorage에 대화 + Stage 저장, 새로고침 후 복원

**선택:** (B) 세션 유지.

**전략적 근거:** §5의 유저 여정이 9단계다. 기존 과제 기본 여정(7단계)에 리스크 프로파일링·보장 갭 분석을 추가한 구조이므로, 한 세션의 대화가 길다. 세션이 유실되면 정보수집부터 다시 시작해야 하고, 데모에서 "리스크 프로파일링 → 갭 분석 → 상품 매칭"이라는 순서 강제 흐름(§3-2) 전체를 보여줄 수 없게 된다.

---

## 9. 데모 시나리오 페르소나 버튼

**선택지:**
- (A) 매번 수동 입력으로 정보수집 진행
- (B) 사전 설계된 페르소나 버튼으로 즉시 시작

**선택:** (B) 2개 페르소나(32세 남 IT 정규직 미혼 / 42세 여 자영업 기혼 자녀2명).

**전략적 근거:** §7에서 우리의 차별화는 "상품이 테이블에 올라오기 전에 작동"하는 것이라 했다. 발표에서 이를 증명하려면 정보수집 → 리스크 진단 → 갭 분석 → 상품 매칭의 전체 흐름을 보여줘야 한다. Stage 1(정보수집)에 시간을 쓰면 핵심인 진단-매칭 흐름을 보여줄 시간이 줄어든다. 두 페르소나는 리스크 프로파일이 다르게 설계되어(미혼 vs 기혼+자녀2, 정규직 vs 자영업, 가족력 무 vs 당뇨+고혈압), 동일 시스템이 다른 진단 결과를 내는 것을 시연할 수 있다 — 이것이 §4-1의 "에이전트가 대화를 통해 정보를 수집하고, 통계 데이터에 매핑하여 리스크 수준을 도출"하는 과정의 증거다.

---

## 10. 모델 선택: google/gemini-2.0-flash-001

**선택지:**
- (A) `anthropic/claude-haiku-4-5` — 빠르고 저렴, 복잡 프롬프트 이행률 불확실
- (B) `anthropic/claude-sonnet-4-5` — 정확, 비용 부담 (타인 API 키)
- (C) `google/gemini-2.0-flash-001` — 빠르고 저렴, 긴 시스템 프롬프트 + 한국어 양호
- (D) `openai/gpt-oss-120b:free` 등 — 보험 상담 용도에 부적합

**선택:** (C) OpenRouter 경유 Gemini Flash.

**전략적 근거:** 우리의 시스템 프롬프트는 9단계 흐름 + 순서 강제 규칙 + 통계 데이터 + 툴 호출 규칙 + 시각화 트리거 + 금소법 준수 규칙이 모두 포함되어 길다. 이 복잡성은 순서의 미분리를 프로세스 레벨에서 해소(§3-2)하기 위해 불가피하다. 모델은 이 프롬프트를 정확히 이행하면서도 비용이 감당 가능해야 한다. 타인의 API 키를 사용하는 상황에서 (B)의 비용은 과도하고, (A)는 복잡한 프롬프트 이행이 불확실했다.

---

## 11. 약관 RAG (벡터 검색)

**선택지:**
- (A) AI의 사전 학습 지식으로 약관 질문에 답변
- (B) 실제 KB라이프 약관을 청크 분할 + 임베딩하여 벡터 검색 후 답변

**선택:** (B) 4,479개 청크, OpenAI text-embedding-3-small, 코사인 유사도 검색.

**전략적 근거:** §4-3에서 "이 상품은 당신의 X 리스크를 커버합니다"라는 설계 근거 제시를 기능 요건으로 명시했다. 상품 매칭 후 Q&A 단계에서 고객이 약관 세부 조건을 물으면, 실제 약관 텍스트를 근거로 답해야 한다. (A)처럼 사전 학습 지식에 의존하면 잘못된 정보를 전달할 수 있고, 이는 §6-2의 보험사 가치("민원 감소")와 정면 충돌한다. §4-4의 설계 근거 리포트가 금소법 설명의무 이행 증거(§6-3)가 되려면, 근거 자체가 실제 약관에 기반해야 한다.

---

## 12. 스테이지 감지 안정화 — 클라이언트 → 서버 명시 전달 (2026-04-08)

**선택지:**
- (A) 서버가 메시지 내 `###STAGE:N###` 마커를 regex 파싱 (기존)
- (B) 클라이언트가 POST body에 `stage: currentStage`를 명시적으로 전달

**선택:** (B) 클라이언트 → 서버 명시 전달, `detectStage()` 제거.

**전략적 근거:** 순서 강제(§3-2)가 충분조건이다. 순서 강제는 스테이지 감지에 의존하고, 스테이지 감지가 틀리면 순서 강제 자체가 무너진다. (A)에서는 최근 10개 메시지만 전송하므로 이전 마커가 유실될 수 있었고, fallback 휴리스틱(메시지 ≤4개 → Stage 2, 그 외 → Stage 3)은 Stage 4 이후를 항상 3으로 감지하는 버그가 있었다. 이는 고지의무 안내(Stage 4) 이후에도 상품 데이터가 전송되지 않아, 상품 매칭(§4-3) 단계가 정상 작동하지 않는 문제로 이어졌다.

**영향 파일:** `ChatInterface.tsx`, `route.ts`

---

## 13. RAG 트리거를 스테이지 기반으로 변경 (2026-04-08)

**선택지:**
- (A) `conversationText.length > 300`이면 RAG 실행 (기존)
- (B) `stage >= 5`일 때만 RAG 실행

**선택:** (B) 스테이지 기반.

**전략적 근거:** §5의 유저 여정에서 약관 검색이 의미 있는 시점은 Stage 5(상품 매칭, §4-3) 이후다. Stage 1~4는 진단 단계이고, 약관(판매 데이터)은 아직 필요 없다. (A)는 진단 단계에서도 판매 데이터를 끌어오는 셈이라, "진단 데이터와 판매 데이터가 소스 자체부터 분리됨"(§4-1)이라는 설계 원칙에 어긋난다. 또한 불필요한 임베딩 API 호출 비용이 발생한다.

**영향 파일:** `route.ts`

---

## 14. 갭 분석 툴화 — calculate_gap_analysis (2026-04-08)

**선택지:**
- (A) AI가 시스템 프롬프트의 기준표를 읽고 직접 계산 (기존 #5의 방식)
- (B) `calculate_gap_analysis` 툴로 결정론적 처리 (`calculate_risk_scores`와 동일 패턴)

**선택:** (B) 툴화. `lib/gapCalculator.ts` 신규 생성.

**설계 결정:**
- 동일 보험 유형 복수 보유 시 covered 점수 합산, 리스크 점수를 상한으로 cap
- 보장금액 1억 미만 -1, 3억 이상 +1 조정은 개별 보험 단위 적용

**전략적 근거:** §6-3에서 "구조화된 프로세스 자체가 설명의무의 실질적 이행 증거"라 했다. 리스크 프로파일링(§4-1)은 이미 `calculate_risk_scores` 툴로 결정론적 처리하는데, 보장 갭 분석(§4-2)만 AI 재량이면 두 단계의 신뢰성에 비대칭이 생긴다. 동일 고객에 대해 매번 다른 갭 결과가 나오면 "구조화된 프로세스"라는 주장이 성립하지 않는다. 기능 셋의 세 단계(§4-1 → §4-2 → §4-3) 전체가 결정론적이어야 "같은 입력 → 같은 진단 → 같은 설계"라는 객관성이 확보된다.

**영향 파일:** `lib/gapCalculator.ts` (신규), `route.ts`, `lib/system-prompt.ts`

---

## 15. doc_type 메타데이터 필터 활용 (2026-04-08)

**선택지:**
- (A) 벡터 유사도만으로 검색 (기존) — `product_id` 필터만 사용
- (B) `doc_type` 필터 추가 — 질문 유형에 따라 약관/상품요약서 우선 검색

**선택:** (B) 키워드 기반 `doc_type` 필터 추가. 면책·약관·특약 등 → `약관`+`사업방법서` / 보험료·진단금 등 → `상품요약서` / 기타 → 필터 없음.

**전략적 근거:** §4-3에서 "각 추천이 어떤 갭을 채우는지 연결해서 보여준다"고 했고, §4-4에서 그 전체를 리포트로 정리해 금소법 이행 증거(§6-3)로 삼는다. 고객이 "면책기간이 어떻게 돼?"라고 물었을 때 상품요약서 청크가 올라오면, 정확한 약관 근거를 제시하지 못한다. 설계 근거의 정확성이 떨어지면, 이행 증거로서의 가치도 떨어진다. 질문 유형에 맞는 문서를 우선 검색해야 §6-2(민원 감소)에도 기여한다.

**영향 파일:** `lib/rag.ts`, `route.ts`

---

## 16. 데이터 파일 모듈 레벨 캐싱 (2026-04-08)

**선택지:**
- (A) 매 요청마다 `readFileSync`로 파일 읽기 (기존)
- (B) 모듈 레벨 변수에 캐싱, 최초 1회만 읽기

**선택:** (B) 모듈 레벨 캐싱.

**전략적 근거:** §5의 9단계 유저 여정에서 한 세션이 최소 9회 이상의 API 요청을 만든다. 매번 파일 I/O를 하면 응답 지연이 누적되고, 이는 #6(스트리밍)에서 확보한 체감 속도를 상쇄한다. 고객이 진단 결과나 갭 분석을 기다리는 시간이 길어지면, 순서 강제(§3-2)의 각 단계에서 이탈이 발생할 수 있다.

**영향 파일:** `route.ts`

---

## 17. Tool 호출 강제 — tool_choice 서버 사이드 지정 (2026-04-08)

**선택지:**
- (A) 프롬프트에서 "반드시 툴을 호출하세요"라고 지시 (기존)
- (B) `tool_choice: 'auto'`를 유지하되 프롬프트를 강화
- (C) Stage 2/3에서 `tool_choice`를 `{ type: 'function', function: { name: '...' } }`로 서버에서 강제 지정

**선택:** (C) 서버 사이드 tool_choice 강제.

**전략적 근거:** 데모 대화 로그(2026-04-08)에서 Stage 3(보장 갭 분석, §4-2)에서 LLM이 `calculate_gap_analysis` 툴을 호출하지 않고 직접 "9만큼의 갭"이라는 무의미한 수치를 생성하는 문제가 확인되었다. (A)는 프롬프트 지시이므로 확률적이고, Gemini Flash에서 이미 실패한 바 있다. (B)도 동일한 한계를 가진다. #14에서 "기능 셋의 세 단계(§4-1 → §4-2 → §4-3) 전체가 결정론적이어야 한다"고 결정했는데, 툴 호출 자체가 확률적이면 결정론적 파이프라인의 진입점이 불안정해진다. (C)는 LLM의 선택을 제거하고 API 레벨에서 강제하므로, 프롬프트 이행률과 무관하게 툴이 호출된다.

**영향 파일:** `route.ts`

---

## 18. 정보 수집 체크리스트 서버 검증 (2026-04-08)

**선택지:**
- (A) 프롬프트에서 "모든 항목을 수집할 때까지 Stage 2로 넘어가지 마세요"라고 지시 (기존)
- (B) 서버에서 대화 텍스트를 정규식으로 검증, 필수 필드 미수집 시 Stage 1로 강제 회귀

**선택:** (B) 서버 사이드 체크리스트 검증. 10개 필드(나이, 성별, 직업, 고용형태, 가족구성, 월 소득, 가족력, 현재 건강상태, 연금 준비, 월 보험료 예산)를 정규식 패턴으로 확인.

**전략적 근거:** 데모 대화 로그에서 고객이 "고민 안해봄"이라고 답했을 때, 연금 현황과 월 예산이 미확인 상태에서 Stage 2로 넘어가는 문제가 확인되었다. §3-2의 순서 강제는 "진단이라는 독립된 단계 자체가 프로세스에 존재"해야 하는데, 정보 수집이 불완전하면 진단 자체의 입력이 불완전하다. #2에서 설계한 통계 기반 수식은 9개 파라미터를 요구하는데, 이 중 하나라도 누락되면 결정론적 점수 산출이 불가능하다. (A)는 프롬프트 지시이므로 LLM이 무시할 수 있고(#17과 동일한 논리), 서버 검증이 유일하게 확실한 방법이다.

**영향 파일:** `lib/validateChecklist.ts` (신규), `route.ts`

---

## 19. 금지 표현 후처리 필터 (2026-04-08)

**선택지:**
- (A) 프롬프트에서 "추천드립니다, 가입하세요 등 판매 권유 표현 금지"라고 지시 (기존)
- (B) 프롬프트 지시 + 서버 출력에서 정규식으로 금지 표현을 규정 준수 표현으로 치환

**선택:** (B) 이중 방어. 14개 금지 패턴("추천드립니다" → "안내드립니다", "가입하세요" → "검토해 보시기 바랍니다" 등)을 후처리 필터로 적용.

**전략적 근거:** 데모 대화 로그에서 LLM이 "우선적으로 고려해 보시는 것을 추천드립니다"를 출력했다. §6-3에서 "구조화된 프로세스 자체가 설명의무의 실질적 이행 증거"라 했는데, 해당 프로세스가 판매 권유 표현을 출력하면 오히려 금소법 위반 증거가 된다. (A)는 확률적이고 이미 실패했다. LLM 출력은 본질적으로 확률적이므로, 규제 준수처럼 100%가 요구되는 영역은 결정론적 후처리로 보장해야 한다. 이는 #14(갭 분석 툴화), #17(Tool 호출 강제)과 동일한 원칙 — "확률적 LLM 의존을 결정론적 코드로 대체"의 연장이다.

**영향 파일:** `lib/complianceFilter.ts` (신규), `route.ts`

---

## 20. 시각화 중복 출력 방지 (2026-04-08)

**선택지:**
- (A) 프롬프트에서 "이미 출력한 시각화를 다시 출력하지 마라" 지시
- (B) 클라이언트에서 직전 시각화와 동일 type이면 렌더링 스킵

**선택:** (B) 클라이언트 사이드 중복 제거.

**전략적 근거:** 데모 대화 로그에서 Stage 2의 리스크맵이 연속 2턴에 걸쳐 출력되었다. 중복 시각화는 사용자 혼란을 야기하고, "이 단계에서 새로운 진단이 이루어진 것인가?"라는 오해를 만든다. (A)는 프롬프트 지시이므로 확률적이다(#17, #18, #19와 동일한 논리). (B)는 이전 메시지 히스토리를 가진 클라이언트에서 처리해야 하는데, parseResponse는 단일 메시지만 처리하므로 ChatInterface에서 메시지 상태를 비교하여 제거한다.

**영향 파일:** `components/ChatInterface.tsx`

---

## 21. 시각화 마커 파서 방어적 처리 (2026-04-08)

**선택지:**
- (A) 프롬프트에서 "코드블록으로 감싸지 마라"고만 지시
- (B) 프롬프트 지시 + 파서에서 코드블록을 벗겨내는 정규식 추가

**선택:** (B) 이중 방어.

**전략적 근거:** 데모 대화 로그에서 4개 시각화 중 3개(갭 분석, 상품 매칭, 최종 보고서)가 LLM이 `###VISUALIZATION###` 마커를 코드블록으로 감싸서 출력하여 파서가 인식에 실패했다. 시각화는 §4-1(리스크맵), §4-2(갭 분석), §4-3(상품 매칭), §4-4(설계 근거 리포트)의 핵심 산출물이다. 이 시각화가 렌더링되지 않으면, 순서 강제(§3-2)의 각 단계에서 고객이 진단 결과를 시각적으로 확인할 수 없고, "납득 있는 가입"이 불가능해진다. (A)만으로는 LLM의 코드블록 출력을 막을 수 없으므로(확률적), 파서가 어떤 형식이 들어오든 처리할 수 있어야 한다.

**영향 파일:** `lib/parseResponse.ts`, `lib/system-prompt.ts`
